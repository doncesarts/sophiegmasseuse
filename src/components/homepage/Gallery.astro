---
import type { Translations } from '../../i18n/types';

interface Props {
  t: Translations;
}

const { t } = Astro.props;

const images = [
  { src: '/images/gallery-1.jpeg', alt: 'Massage studio interior at Voyage Sensoriel in Zurich' },
  { src: '/images/placeholders/gallery-2.svg', alt: 'Treatment room at the Voyage Sensoriel massage studio in Zurich' },
  { src: '/images/placeholders/gallery-3.svg', alt: 'Relaxation area at the Voyage Sensoriel massage studio in Zurich' },
];
---

<section class="section container">
  <div class="max-w-2xl mb-10">
    <h2>{t.gallery.title}</h2>
    <p class="text-base-content/80">{t.gallery.subtitle}</p>
  </div>
  <div class="grid grid-cols-1 sm:grid-cols-3 gap-6">
    {images.map((image) => (
      <img src={image.src} alt={image.alt} width="1200" height="900" loading="lazy" class="w-full h-auto rounded-box object-cover" />
    ))}
  </div>
</section>
